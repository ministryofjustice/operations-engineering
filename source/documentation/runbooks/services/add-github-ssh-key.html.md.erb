---
owner_slack: "#operations-engineering-alerts"
title: Adding an SSH Key to Github
last_reviewed_on: 2022-10-20
review_in: 12 months
---

# Adding an SSH Key to Github

`ssh-keygen -t ed25519 -C "name@digital.justice.gov.uk"`
Press enter until the SSH key is created.
`eval "$(ssh-agent -s)"`
`ssh-add ~/.ssh/id_ed25519`
`pbcopy < ~/.ssh/id_ed25519.pub`
Paste the text somewhere and it should be in this format: `ssh-ed25519 HEX-VALUE name@digital.justice.gov.uk`
Add the new ssh key in `https://github.com/settings/keys`
Click on the `Configure SSO` button to authorise the key.
You should recieve an email that a key was added to your account.
Try `git clone git@github.com:ministryofjustice/moj-org-discussions.git`
